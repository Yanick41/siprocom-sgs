{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "installCommand": "npm --prefix server install && npm --prefix client install",
  "buildCommand": "npm --prefix client run build",
  "outputDirectory": "client/dist",
  "framework": null,

  "functions": {
    "api/index.js": {
      "maxDuration": 30,

      "//": [
        "Only the Linux query engine is force-included. Vercel traces static",
        "requires on its own, so node_modules does not need listing — and must",
        "not be: server/node_modules is 476 MB against a 250 MB limit per",
        "function, so a broad glob fails at packaging, not at runtime.",
        "",
        "The engine itself is loaded dynamically by Prisma at first query, so",
        "the tracer cannot see it. Miss it and the build succeeds while every",
        "request dies on 'Query engine library not found' — a deploy that is",
        "green and broken."
      ],
      "includeFiles": "server/node_modules/.prisma/client/libquery_engine-rhel-openssl-3.0.x.so.node"
    }
  },

  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],

  "crons": [{ "path": "/api/alerts/sweep", "schedule": "0 6 * * *" }]
}
